{
  "name": "ai-screen",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "pretty": "prettier -u --write \"**/*\" --log-level warn"
  },
  "devDependencies": {
    "eslint": "^9.33.0",
    "prettier": "^3.6.2"
  }
}
